import {Board} from "./board";

export function minimax(board: Board, depth: number, maximizingPlayer: number) {
    const winner = board.getWinner();
    if (depth === 0 || winner !== 0) {
        if (winner === )
    }
}